[local]
localhost ansible_connection=local

[jenkins]
# Populated after terraform apply — update JENKINS_IP below, then run configure-jenkins.yaml
# jenkins_host ansible_host=JENKINS_IP ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/YOUR_KEY.pem
#
# Shortcut (run directly without editing this file):
#   JENKINS_IP=$(cd terraform && terraform output -raw jenkins_public_ip)
#   ansible-playbook ansible/playbooks/configure-jenkins.yaml \
#     -i "${JENKINS_IP}," \
#     --user ec2-user \
#     --private-key ~/.ssh/YOUR_KEY.pem
